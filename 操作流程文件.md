# CVTA 公文 OCR 自動化系統 - 操作流程文件

## 一、啟動系統
1. 執行 `python -m uv run src/ui/main_qml.py` 啟動 GUI
2. 系統自動載入 `config/settings.py` 中的設定（Ollama URL、模型名稱等）
3. 若 Ollama 服務未運行，終端機會顯示提示訊息

## 二、新增公文流程
1. 點擊頂部「新增」按鈕 → 觸發檔案選擇對話框
2. 選取多張圖片後，呼叫 `StorageBackend.add_document(image_paths, metadata)`
3. 每張圖片路徑與對應的欄位資料（doc_date、doc_category、doc_number、doc_from、key_points）寫入 JSON 歸檔
4. 表格與側邊表單同步更新，顯示新增公文

## 三、分析公文流程
1. 點擊頂部「分析公文」按鈕 → 呼叫 `DocController.analyzeDocument()` slot
2. `analyzeDocument` 從 `StorageBackend._documents_list` 取得所有公文的 `image_source`
3. 篩選出尚未分析的文件，呼叫 `DocumentProcessor.process_batch(image_paths)`
4. `process_batch` 並列執行每張圖片：
   - OCR + 欄位提取（Ollama 多模態模型）
   - 資料驗證（validator.py）
   - 結果回傳包含 `success`、`csv_data`、`error_code`
5. 批次處理結果寫回 `StorageBackend`，表格與側邊表單同步更新

## 四、上傳公文流程
1. 點擊頂部「上傳公文」按鈕 → 呼叫 `DocController.uploadDocuments()` slot
2. 自動遍歷 `StorageBackend._documents_list` 中所有已分析完成的公文圖片路徑
3. 對每張圖片：
   - 上傳至 Google Drive 指定資料夾（`GoogleDriveService.upload_file`）
   - 取得 `file_id` 後，呼叫 `GoogleSheetsService.append_row` 將公文資訊與 `file_id` 寫入雲端試算表
4. 上傳進度與結果同步更新至 UI

## 五、歸檔功能
- **開啟歸檔**：讀取本地 JSON 檔案（`StorageBackend.load_from_json`），重新載入表格
- **儲存歸檔**：將當前所有公文資料寫入本地 JSON（`StorageBackend.save_to_json`）

## 六、清除資料
點擊「清除所有資料」→ `StorageBackend.clear()` → 表格與側邊表單清空

## 七、設定頁面
`openSettings` 目前僅為入口，實際設定功能待後續加入（包含 Ollama 模型、prompts.json 路徑等）

---

**注意事項：**
- 分析公文需確保 Ollama 服務運行且模型已載入
- 上傳公文需先完成 Google Workspace API 認證（Phase 2）
- 所有公文欄位資料均透過 `DocumentProcessor` 的 prompts.json 規範解析